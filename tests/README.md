# Tests

Suggested future tests:

- parser test with minimal Cowrie JSONL input
- parser test with minimal DDoSPot JSON and text input
- statistical script test using `data/paired_observations_example.csv`
